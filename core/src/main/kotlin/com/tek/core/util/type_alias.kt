package com.tek.core.util

import com.tek.core.TekProperties

/**kotlin typealias referencing properties
 *
 * stored in application-{[com.tek.core.SpringProfile]}.yaml and referencing [TekProperties] */
typealias TekProperty = String
